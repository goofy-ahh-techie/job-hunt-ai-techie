fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
httpx==0.27.2
python-dotenv==1.0.1
# Cosine similarity for Phase 4 semantic matching: embeddings are compared as a
# single normalised matrix product rather than in a Python loop.
numpy==2.1.1

# --- test-only ---
pytest==8.3.3
pytest-asyncio==0.24.0
